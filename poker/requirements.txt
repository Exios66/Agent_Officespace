# Legacy MVP: default install.
#
# ------------------------------------------------------------------
# NOTE: This file is a backwards-compatible passthrough. Prefer the
# feature-scoped files under `poker/requirements/` when you can:
#
#     pip install -r poker/requirements/base.txt      # just preprocess/features
#     pip install -r poker/requirements/ml.txt        # + XGBoost / LightGBM
#     pip install -r poker/requirements/nn.txt        # + PyTorch MLP / LSTM
#     pip install -r poker/requirements/llm.txt       # + LoRA fine-tuning
#     pip install -r poker/requirements/viz.txt       # + plotting
#     pip install -r poker/requirements/tracking.txt  # + wandb / tensorboard
#     pip install -r poker/requirements/dev.txt       # + jupyter / pytest / black
#     pip install -r poker/requirements/all.txt       # + everything at once
#
# This file re-exports the "run every training path" superset so that
# users following the existing `pip install -r poker/requirements.txt`
# instruction keep getting the same behaviour.
# ------------------------------------------------------------------

-r requirements/all.txt
