# Legacy MVP: developer tooling.
#
# Install with:
#     pip install -r poker/requirements/dev.txt
#
# Adds Jupyter, IPython, black, and pytest — everything needed to
# execute the notebooks under `poker/notebooks/` and run the test
# suite under `poker/tests/`.
-r base.txt

jupyter>=1.0.0
ipython>=8.12.0
black>=23.3.0
pytest>=7.3.0
