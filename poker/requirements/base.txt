# Legacy MVP: base runtime dependencies.
#
# The minimum you need to run:
#   - src/data/preprocess.py
#   - src/features/engineering.py
#   - scripts/download_data.py
#
# Install with:
#     pip install -r poker/requirements/base.txt
#
# Everything else (ML, NN, LLM, viz, tracking, dev) is opt-in via the
# sibling files in this directory.

# Core scientific stack
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Hugging Face — needed for the PokerBench download
huggingface-hub>=0.16.0
datasets>=2.14.0

# Storage / IO
pyarrow>=12.0.0
fastparquet>=2023.4.0

# Utilities
tqdm>=4.65.0
pyyaml>=6.0
python-dotenv>=1.0.0
requests>=2.31.0
